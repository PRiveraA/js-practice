add7 = (n) => {
  return n + 7
}

multipy = (a, b) => {
  return a * b
}

capitalize = (s) => {
  let sCapped = s.charAt(0).toUpperCase() + s.slice(1)
  return sCapped
}

lastLetter = (s) => {
  length = s.length
  return s.charAt(length - 1)
}